extends layout

block content
    #content
        script(src="/javascripts/dynamic_form.js")
        script(src="/javascripts/form2js.js")
        p Please be sure to fill in all values before submitting
            br
            div
                form(id="profile", action='/profile', method='post')
                    div(id="services")
                        h4 Profile
                        input(type="text", name="profile_name", placeholder="Profile name")
                        br
                        br
                        button(type='button', class="addService", name="add_service", onclick='addService()') Add Service
                        button(type='button', class="submit_top", name ="send_profile", onclick='convertForm()') Send Profile
                        br
                        br
                    button(type='button', class="submit", name ="send_profile", onclick='convertForm()') Send Profile
